# URL of the FastAPI backend (see backend/README.md). For local dev this is
# usually http://localhost:8000; in Vercel project settings, point it at the
# deployed backend (e.g. Render) URL.
VITE_API_URL=http://localhost:8000
